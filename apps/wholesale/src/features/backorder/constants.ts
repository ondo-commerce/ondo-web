/**
 * 좌측 미송 목록의 6열 폭 — `SKU / 상품명 / 색상 / 사이즈 / 총 미송 수량 / 예상 입고일`.
 *
 * `Table`을 쓰지 않는 이유: 행 하나하나가 아코디언(버튼 + 펼침 본문)이라
 * `<tr>` 안에 들어가지 않는다. 대신 **표 머리 행과 아코디언 행이 이 상수 하나를 같이 써서**
 * 열을 맞춘다 — 한쪽만 고치면 목록 전체가 어긋난다.
 *
 * 상품명만 남는 폭을 먹는다. 나머지는 값의 길이가 정해져 있어 고정 폭이 읽기 쉽다.
 */
export const SKU_GRID =
  "grid grid-cols-[6rem_minmax(8rem,1fr)_5rem_4rem_6.5rem_6.5rem] items-center gap-3";

/** 등록되지 않은 예상 입고일의 표시. Figma 좌측 목록 12행이 전부 이 값이다 */
export const EMPTY_MARK = "-";
