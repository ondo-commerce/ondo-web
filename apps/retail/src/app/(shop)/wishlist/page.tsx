import type { Metadata } from "next";
import { PlaceholderPage } from "@/shared/components/PlaceholderPage";

export const metadata: Metadata = { title: "찜 목록" };

export default function Page() {
  return <PlaceholderPage title="찜 목록" />;
}
