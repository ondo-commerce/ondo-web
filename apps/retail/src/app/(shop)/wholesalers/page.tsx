import type { Metadata } from "next";
import { PlaceholderPage } from "@/shared/components/PlaceholderPage";

export const metadata: Metadata = { title: "거래처 관리" };

export default function Page() {
  return <PlaceholderPage title="거래처 관리" />;
}
