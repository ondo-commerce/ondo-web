import type { Metadata } from "next";
import { PlaceholderPage } from "@/shared/components/PlaceholderPage";

export const metadata: Metadata = { title: "주문 완료" };

export default function Page() {
  return <PlaceholderPage title="주문 완료" />;
}
