/**
 * 셸이 쓰는 더미 값. API가 붙으면 이 파일만 지운다.
 *
 * 장바구니 숫자는 여기 없다 — 담긴 목록을 실제로 들고 있는
 * `features/cart/store.ts`에서 뱃지와 본문이 같이 읽는다. 고정값 하나를 더 두면
 * 목록에서 지운 뒤에도 뱃지가 안 따라와서, 원본의 §6-4 결함(같은 화면에서
 * 헤더 `6개 담김` · 뱃지 `4` · 본문 `담긴 조합 4개`)이 코드로 옮겨온다.
 */

/** 로그인한 소매 사장의 표시 정보. 이니셜은 상호 첫 글자다 */
export const SHELL_ACCOUNT = {
  storeName: "봄봄상회",
  initial: "봄",
} as const;
