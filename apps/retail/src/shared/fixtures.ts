/**
 * 셸이 쓰는 더미 값. API가 붙으면 이 파일만 지운다.
 *
 * 지금은 비어 있다. 셸이 직접 아는 더미가 하나도 없기 때문이다.
 *
 * - 장바구니 숫자는 서버(`GET /cart-items/count`)가 준다. `app/(shop)/layout.tsx`가
 *   받아 `features/cart`의 `CartButton`에 넘긴다. 고정값을 여기 하나 더 두면 목록에서
 *   지운 뒤에도 뱃지가 안 따라와서, 원본의 §6-4 결함(헤더 `6개 담김` · 뱃지 `4` ·
 *   본문 `담긴 조합 4개`)이 코드로 옮겨온다.
 * - 상호명(`SHELL_ACCOUNT`)은 `features/account/fixtures.ts`의 `SETTINGS_ACCOUNT`
 *   하나로 모았다. 설정 화면이 고치는 값을 셸이 한 벌 더 들고 있으면, 저장한
 *   순간 헤더와 본문이 같은 세션에서 다른 이름을 말한다(`retail-market` F5).
 *
 * 파일을 지우지 않고 남겨 둔 이유: 셸이 다시 더미를 갖게 되면 자리가 여기라는
 * 것과, 왜 두 값이 여기서 빠져나갔는지를 같이 남기기 위해서다.
 */

export {};
