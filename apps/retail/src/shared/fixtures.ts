/**
 * 셸이 쓰는 더미 값. API가 붙으면 이 파일만 지운다.
 *
 * 헤더 장바구니 숫자를 여기 한 곳에 두는 이유: Figma 원본은 같은 화면에서
 * 헤더 접근성 이름 `6개 담김` · 뱃지 `4` · 본문 `담긴 조합 4개`가 서로 달랐다
 * (`retail_screen_spec.md` §6-4). 값이 두 곳에 적히면 그 결함이 코드로 옮겨온다.
 */

/** 로그인한 소매 사장의 표시 정보. 이니셜은 상호 첫 글자다 */
export const SHELL_ACCOUNT = {
  storeName: "봄봄상회",
  initial: "봄",
} as const;

/**
 * 장바구니에 담긴 **SKU 조합 수**(색상 × 사이즈). 장 수가 아니다.
 * 뱃지 숫자와 장바구니 버튼의 접근성 이름이 둘 다 이 값을 읽는다.
 */
export const CART_ITEM_COUNT = 4;
