packages:
  - "apps/*"
  - "packages/*"

# postinstall 스크립트 실행을 허용할 패키지 (pnpm은 공급망 공격 방어로 기본 차단한다)
allowBuilds:
  msw: false # postinstall이 안내문만 찍는다. 빌드 없음
  sharp: true # Next.js 이미지 최적화 네이티브 바이너리
  unrs-resolver: true # ESLint import 해석기 네이티브 바이너리
